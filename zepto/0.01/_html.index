<!DOCTYPE HTML>
<html>
<head>
  <meta name="viewport" content="user-scalable=no, initial-scale=1, maximum-scale=1, minimum-scale=1, width=device-width" />
  <!-- http://stackoverflow.com/questions/19250910/viewport-argument-value-device-width-for-key-width-is-invalid-and-has-been -->
  <meta http-equiv="Content-type" content="text/html; charset=utf-8">
  <title>XUI</title>
  <script src="//cdnjs.cloudflare.com/ajax/libs/zepto/1.1.3/zepto.min.js"></script>
  <script type="text/javascript">
  Zepto(function($){

    function getWord(){
      var s = []
      var string ='';

      if ($('#addWord').val() == '') {
        alert("empty");
      }
      else {
        $('.word').on('click', function(e){
          var word = $(this).text()
          $('#results').append('<div class="' + word +' result" "><a href="#" class="item">X</a> ' + word + '</div>');
        });
        $(document).on('click','#submit_word',function(){
          if($('#addWord').val() == "")
          {
            alert("empty")
          }
          else {
            $('#results').append('<div class="result" "><a href="#" class="item">X</a> ' + $('#addWord').val() + '</div>');
          }
        });
        $(document).on('click','.result',function(){
          // $(this).parent().remove();
          $(this).remove();
        });
        $(document).on('click','#restart',function(){
          $('.result').remove();
        });
      }
    };

    // function getLetter(){
    //   var s = [];
    //   var string ='';

    //   if (false) {

    //   }
    //   else {
    //     $('.letter').on('click', function(e){
    //       b = $(this).text();
    //       s.push(this, b);
    //       console.log(s);
    //       var letter = $(this).text()
    //       $('#resultsPreview').append('<div class="' + letter +' result" "><a href="#" class="item">X</a> ' + letter + '</div>');

    //     });
    //     $(document).on('click','#letter',function(){
    //       var word = [];
    //       for (var i = 0; i < s.length; i += 2) {
    //         s[i+1] && word.push(s[i + 1]);
    //       }
    //       a = word.join();
    //       word = a.replace(/\,/g,'');
    //       if(word.length === 0)
    //       {
    //         alert("empty")
    //       }
    //       else {
    //         $('#resultsLetter').append('<div><a href="#" class="item">X</a> ' + word + '</div>');
    //       }
    //       s = [];
    //       $('#resultsPreview').html(''); // clear preview letters
    //     });
    //     $(document).on('click','.item',function(){
    //       $(this).parent().remove();
    //     });
    //     $(document).on('click','#clear',function(){
    //       $('#resultsLetter').html('');
    //     });
    //     $(document).on('click','#backspace',function(){
    //       for (var i = 0; i < s.length; i += 2) {
    //         s.splice(-1, i+1);
    //       }
    //       console.log(s);
    //     });
    //   }
    // };

    getWord();

  })
</script>
</head>
<body>
  <ul>
    <li class="word">Mig</li>
    <li class="word">Læge</li>
    <li class="word">Sygeplejerske</li>
    <li class="word">Arbejde</li>
    <li class="word">Familie</li>
  </ul>

  <ul>
    <li class="word">:-D</li>
    <li class="word">:-)</li>
    <li class="word">:-|</li>
    <li class="word">:-/</li>
    <li class="word">:-(</li>
  </ul>

  <ul>
    <li class="word">Klør</li>
    <li class="word">Drikke</li>
    <li class="word">Sulten</li>
    <li class="word">Smerte</li>
    <li class="word">Toilet</li>
  </ul>
  <!-- we add to our foodList from the value of the following input -->
  <input type="text" value="write a text" id="addWord" />

  <!-- we call addToFood(); through the following button  -->
  <input id="submit_word" type="submit" value="submit">
  <input id="restart" type="submit" value="restart">

  <!-- The list of food is displayed in the following div -->
  <div id="results"></div>
<!--   <ul>
    <li class="letter">a</li>
    <li class="letter">b</li>
    <li class="letter">c</li>
    <li class="letter">d</li>
    <li class="letter">e</li>
  </ul>
  <h1>Word</h1>
  <div id="resultsLetter"></div>
  <h1>Previw</h1>
  <div id="resultsPreview"></div>

  <input id="letter" type="submit" value="submit">
  <input id="backspace" type="submit" value="backspace">
  <input id="clear" type="submit" value="clear word">
  <div id="output"></div> -->



</body>
</html>